# 如何在个人博客上发布一篇文章

> 从新建文件到博客上线,完整操作流程。基于本项目(Vue 3 + Vite + vite-ssg + GitHub Pages)。

## 流程总览

```
1. 写 .md 文件(在 src/content/posts/)
2. (可选) 准备封面图(放 public/images/)
3. 本地预览(可选)
4. git add + commit + push
5. GitHub Actions 自动构建部署
6. 几分钟后文章上线
```

## 第 1 步:创建 Markdown 文件

在 `src/content/posts/` 目录下新建一个 `.md` 文件,文件名会作为 URL 的 slug:

```
src/content/posts/
  ├── 2025我的年度总结.md
  ├── 如何在个人博客上发布一篇文章.md
  └── my-new-article.md       ← 新建这个
```

### 文件名建议

- 用 ASCII 或中文都可(都会 URL 编码)
- 简短、描述性、不带空格(用 `-` 替代)
- 例:`react-19-新特性.md` 而非 `React 19 新特性.md` 更稳

## 第 2 步:写 front-matter(必须)

文件顶部必须有 `---` 包裹的 YAML:

```markdown
---
title: 我的第一篇新文章
date: 2026-08-22 14:30:00
tags: [生活, 思考]
categories: [随笔]
cover: /medias/featureimages/0.webp
top: false
description: 一句话简介,会显示在首页卡片摘要(选填)
---

# 正文开始
```

### 字段说明

| 字段 | 必填 | 说明 |
|---|---|---|
| `title` | ✅ | 文章标题 |
| `date` | ✅ | `YYYY-MM-DD HH:mm:ss`,**决定 URL 路径**,即 `/YYYY/MM/DD/slug/` |
| `tags` | ⭕ | 数组,影响标签云和归档分类 |
| `categories` | ⭕ | 数组或字符串,影响分类页 |
| `cover` | ⭕ | 封面图路径(`/medias/featureimages/N.webp` 会按 slug 哈希自动选;或自己指定) |
| `top` | ⭕ | `true` 时进首页「推荐文章」区 |
| `description` | ⭕ | 自定义摘要;不填则从正文前 90 字符截取 |

> ⚠️ **`date` 时间格式**:本地时区(无时区后缀)→ Asia/Shanghai 解析。
> 写 `2026-08-22 14:30:00` = 2026-08-22 14:30 +08:00 = 2026-08-22 06:30 UTC。
> URL 路径按本地日期生成,即 `/2026/08/22/...`。

## 第 3 步:写正文(Markdown)

正文写在第二个 `---` 之后。支持标准 Markdown + 代码块:

```markdown
## 二级标题

正文段落,支持 **粗体** *斜体* [链接](https://example.com)。

### 三级标题

- 无序列表
- 列表项

1. 有序列表
2. 第二项

> 引用文本

\`\`\`bash
# 代码块带 Shiki 高亮
npm run build
\`\`\`

\`\`\`typescript
// TypeScript 也支持
const greeting: string = 'hello'
\`\`\`
```

### 插入图片

```markdown
![描述](图片路径)
```

图片放 `public/images/` 下,引用时用 `/images/xxx.webp`(注意**以 `/` 开头**)。

> 💡 本项目所有图片已用 sharp 压缩为 WebP。新加图片建议:
> 1. 用 [squoosh.app](https://squoosh.app) 或 `sharp` 转 WebP
> 2. 推荐尺寸:封面 1280×720,正文图 800~1200 宽
> 3. **先** 跑 `node scripts/compress-images.mjs` 重新生成 `src/__generated_routes.ts`

## 第 4 步:本地预览(强烈建议)

```bash
npm run dev
# 打开 http://127.0.0.1:5173/
```

- 首页应能看到新文章卡片
- 点进去能打开详情页
- 左侧文章目录(PC 宽屏)应显示从 h2/h3 提取的标题
- 代码块应高亮(Shiki)
- 搜索(导航栏 🔍 或 `Ctrl+K`)能找到你的标题

预览 OK 后再 push。

## 第 5 步:提交并推送

```bash
git add src/content/posts/我的新文章.md
git commit -m "post: 我的新文章"
git push
```

> 如果改了别的文件(`public/` 加了图、改了 `site.ts` 等),都一起 add:
> ```bash
> git add .
> git commit -m "post: xxx + cover image"
> git push
> ```

## 第 6 步:等自动部署

推送后:

1. 打开 https://github.com/liangyoubiao/liangyoubiao.github.io/actions
2. 看 "Build & Deploy to GitHub Pages" workflow 跑(1-2 分钟)
3. 绿色 ✅ 后,访问 https://liangyoubiao.github.io
4. 新文章应在首页「最新文章」或「推荐文章」(如果 `top: true`)里

## 完整示例

`src/content/posts/react-19-新特性.md`:

```markdown
---
title: React 19 新特性速览
date: 2026-08-22 10:00:00
tags: [React, 前端, 笔记]
categories: [技术]
cover: /medias/featureimages/3.webp
top: false
description: React 19 来了,本文聊聊几个值得关注的新特性。
---

# React 19 新特性速览

React 19 终于发布了,本文梳理几个我比较关注的新变化。

## Actions

\`\`\`typescript
async function updateUser(user: User) {
  "use server"
  await db.users.update(user)
}
\`\`\`

## use 钩子

\`\`\`typescript
const data = use(fetch("/api/data"))
\`\`\`
```

推送:

```bash
git add src/content/posts/react-19-新特性.md
git commit -m "post: React 19 新特性速览"
git push
```

3 分钟后,文章出现在 https://liangyoubiao.github.io/2026/08/22/react-19-新特性/

## 进阶用法

### 置顶文章(进「推荐文章」)

```yaml
top: true
```

### 封面图

**选项 A:用系统自带池**(24 张图随机)
```yaml
cover: /medias/featureimages/0.webp   # 或 0~23 任一
```

**选项 B:用哈希选** — 不填 `cover` 时,系统按 slug 哈希自动从 24 张里选一张

**选项 C:自定义封面**
1. 把图放进 `public/images/`(或 `public/medias/featureimages/`)
2. 推荐 WebP 格式,1280×720 左右
3. front-matter 写:
```yaml
cover: /images/我的封面.webp
```

### 草稿(先不发但要本地预览)

写一个文件,先用本地 dev 看看,改好再 push 即可。没有"草稿模式"开关,简单粗暴。

### 配图放哪

```
public/
  ├── images/                    # 你自己加的图
  │   └── my-photo.webp
  ├── medias/featureimages/      # 系统池
  └── images/yuque/              # 老的语雀图(可删)
```

正文里引用:
```markdown
![alt](/images/my-photo.webp)
```

### 修改已有文章

直接编辑对应的 `.md` 文件,改完 commit push,几分钟内线上更新。

### 删除文章

```bash
git rm src/content/posts/要删的.md
git commit -m "chore: 删除旧文"
git push
```

## 常见问题

### Q: 文章不显示在首页?
- 检查 `date` 是不是未来时间 — 首页按发布时间倒序,未来时间排到最后
- 检查 `src/content/posts/` 路径正确
- 清浏览器缓存或 SW:DevTools → Application → Clear storage

### Q: 推送后 GitHub Actions 失败?
看 Actions 日志,最常见:
- `npm ci` 失败 → `package-lock.json` 损坏,删了重 `npm install`
- `sh: command not found` → workflow 文件被破坏
- `Cannot find module` → 依赖缺失

### Q: 代码块不高亮?
- 确认用了 ` ``` ` 三个反引号 + 语言名:` ```typescript `
- 已支持的语言见 `src/utils/markdown.ts` 的 `COMMON_LANGS`
- 没在列表的语言会回退到 `plaintext`

### Q: 文章目录(TOC)不显示?
- 文章需要至少一个 `##` 或 `###` 标题才会生成 TOC
- 短文章无 TOC 是正常的
- 屏幕 < 1100px 也会自动隐藏(右侧空间不够)

### Q: 搜索搜不到刚发的文章?
- `npm run build` 时会自动跑 `build-search.mjs` 生成 `public/search-data.json`
- 推完 search-data.json 才会更新索引
- 等下一次 push 就有了(或者 `git add public/search-data.json` 单独 commit)

## 速查表

```bash
# 1. 写文章
# 编辑器新建 src/content/posts/xxx.md,加 front-matter

# 2. 本地预览(可选)
npm run dev
# → http://127.0.0.1:5173

# 3. 提交推送
git add src/content/posts/xxx.md
git commit -m "post: xxx"
git push

# 4. 等 ~2 分钟
# → https://liangyoubiao.github.io/YYYY/MM/DD/xxx/
```

## 自动化进阶(可选)

### 一键脚本(可选,放在项目根 `new-post.sh`)

```bash
#!/bin/bash
read -p "文章标题: " title
read -p "tags (逗号分隔): " tags
slug=$(echo "$title" | tr ' ' ' '-' | tr '[:upper:]' '[:lower:]')
date=$(date "+%Y-%m-%d %H:%M:%S")
file="src/content/posts/${slug}.md"
cat > "$file" << EOF
---
title: $title
date: $date
tags: [$(echo $tags | sed 's/,/, /g')]
---

# $title

正文开始写这里。
EOF
echo "已创建: $file"
```

## 参考

- `src/content/posts/` — 现有文章示例
- `src/utils/site.ts` — 站点全局配置
- `src/__generated_routes.ts` — 构建时自动生成,每次 `npm run build` 后覆盖
- `README.md` — 项目总览

